# Dependencies
node_modules/
pnpm-lock.yaml.bak

# Build output
web-dist/
*.tsbuildinfo

# Local data — SQLite, file cache, agent state
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Environment
.env
.env.local

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Local-only design / planning notes — kept on disk for reference, not
# part of the public repo (kickoff materials, mockup HTML, etc.).
DesignVocab/
